import LoadingSpinner from '../../../shared/components/atom/LoadingSpinner/LoadingSpinner';

export default function SearchLoadPage() {
  return (
    <div className="loader-container">
      <LoadingSpinner />
    </div>
  );
}
